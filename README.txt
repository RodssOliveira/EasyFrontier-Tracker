Para executar o projeto é necessário digitar o seguinte comando no prompt

pip install -r requeriments.txt na pasta onde está o requeriments.

Para executar o projeto é necessário digitar na mesma pasta em que o arquivo manage.py se encontra o seguinte comando: python manage.py runserver e acessar o site 127.0.0.1:8000